name: hello world
on:
    push:
    pull_request:
      branches:
        - master

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: hello step
        run: |
          echo "welcome to gha"
          echo "welcome to build"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test step
        run:  echo "Welcome to test"
        
      
    

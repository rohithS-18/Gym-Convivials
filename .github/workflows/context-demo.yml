name: context-demo
on: workflow_dispatch
jobs:
  context-info:
    runs-on: ubuntu-latest
    steps:
      - name: print context details
        run: echo "${{github.actor}} triggered from ${{github.ref_name}} branch"
